# RAGAS 오프라인 평가 전용 의존성.
#
# ⚠️ 앱 requirements.txt와 같은 환경에 설치하지 마세요 —
# ragas 0.1.x는 구버전 langchain을 요구해서 앱이 고정한
# langchain==0.3.x와 의존성이 충돌합니다 (같이 설치하면 pip이
# langchain을 조용히 다운그레이드해 앱을 깨뜨립니다).
#
# 사용법 (백엔드 컨테이너 안에서 일회성 설치 후 실행):
#   docker compose exec fastapi pip install -r eval/requirements-eval.txt
#   docker compose exec fastapi python -m eval.harness --smoke
# (컨테이너 재생성 시 초기화되므로 앱 환경 오염 걱정 없음)
ragas==0.1.21
